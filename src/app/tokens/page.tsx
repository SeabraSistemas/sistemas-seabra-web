import { TokensPainel } from '@/components/tokens/TokensPainel';

/** Cota diária do limite semanal do Claude. Tudo no navegador — ver src/lib/tokens/estado.ts. */
export default function TokensPage() {
  return <TokensPainel />;
}
